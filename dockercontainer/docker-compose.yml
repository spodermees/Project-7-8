version: '3'
services:
  web:
    image: nginx:alpine  # Use a lightweight NGINX version for Raspberry Pi
    container_name: nginx_pi
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html  # Custom HTML files
    networks:
      - webnet

networks:
  webnet:
